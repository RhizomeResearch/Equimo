<!-- Generated by docs/finetuning/generate_api_reference.py. Do not edit. -->

# Recipes and profiles

Fine-tuning recipes, staged workflows, and declared fidelity profiles.

[Fine-tuning overview](../index.md) | [API reference index](index.md)

## Symbols

- [`adapter_houlsby2019_profile`](#equimo-finetune-adapter-houlsby2019-profile)
- [`adapter_transformer`](#equimo-finetune-adapter-transformer)
- [`adapter_transformer_strong`](#equimo-finetune-adapter-transformer-strong)
- [`adapterfusion_pfeiffer2021_profile`](#equimo-finetune-adapterfusion-pfeiffer2021-profile)
- [`adaptformer_chen2022_profile`](#equimo-finetune-adaptformer-chen2022-profile)
- [`adaptformer_transformer`](#equimo-finetune-adaptformer-transformer)
- [`attention_pool_probe`](#equimo-finetune-attention-pool-probe)
- [`available_profile_ids`](#equimo-finetune-available-profile-ids)
- [`boft_liu2024_profile`](#equimo-finetune-boft-liu2024-profile)
- [`continued_ssl_adaptation`](#equimo-finetune-continued-ssl-adaptation)
- [`continued_ssl_trainable_spec`](#equimo-finetune-continued-ssl-trainable-spec)
- [`ContinuedSSLAdaptationConfig`](#equimo-finetune-continuedssladaptationconfig)
- [`ContinuedSSLPlan`](#equimo-finetune-continuedsslplan)
- [`dora_factored_2026_profile`](#equimo-finetune-dora-factored-2026-profile)
- [`dora_liu2024_profile`](#equimo-finetune-dora-liu2024-profile)
- [`dora_nvlabs_reference_profile`](#equimo-finetune-dora-nvlabs-reference-profile)
- [`eva_initializer_profile`](#equimo-finetune-eva-initializer-profile)
- [`FineTuneStage`](#equimo-finetune-finetunestage)
- [`fourierft_gao2024_profile`](#equimo-finetune-fourierft-gao2024-profile)
- [`full_ft_llrd`](#equimo-finetune-full-ft-llrd)
- [`head_plus_norm`](#equimo-finetune-head-plus-norm)
- [`HeadPlusNormConfig`](#equimo-finetune-headplusnormconfig)
- [`ia3_liu2022_profile`](#equimo-finetune-ia3-liu2022-profile)
- [`linear_probe`](#equimo-finetune-linear-probe)
- [`LinearProbeConfig`](#equimo-finetune-linearprobeconfig)
- [`LinearProbeRecipe`](#equimo-finetune-linearproberecipe)
- [`lora_equimo_default_profile`](#equimo-finetune-lora-equimo-default-profile)
- [`lora_fa_zhang2026_profile`](#equimo-finetune-lora-fa-zhang2026-profile)
- [`lora_hu2021_qv_profile`](#equimo-finetune-lora-hu2021-qv-profile)
- [`lora_transformer`](#equimo-finetune-lora-transformer)
- [`lora_transformer_all_linear`](#equimo-finetune-lora-transformer-all-linear)
- [`lpft`](#equimo-finetune-lpft)
- [`LPFTRecipe`](#equimo-finetune-lpftrecipe)
- [`oft_qiu2023_profile`](#equimo-finetune-oft-qiu2023-profile)
- [`partial_ft_last_k_blocks`](#equimo-finetune-partial-ft-last-k-blocks)
- [`partial_unfreeze`](#equimo-finetune-partial-unfreeze)
- [`PartialUnfreezeConfig`](#equimo-finetune-partialunfreezeconfig)
- [`pissa_meng2024_profile`](#equimo-finetune-pissa-meng2024-profile)
- [`prefix_tuning_li2021_profile`](#equimo-finetune-prefix-tuning-li2021-profile)
- [`prepare_lpft_stage1_model`](#equimo-finetune-prepare-lpft-stage1-model)
- [`prepare_lpft_stage2_model`](#equimo-finetune-prepare-lpft-stage2-model)
- [`ptuning_v2_liu2022_profile`](#equimo-finetune-ptuning-v2-liu2022-profile)
- [`randlora_profile`](#equimo-finetune-randlora-profile)
- [`recipes`](#equimo-finetune-recipes)
- [`rslora_kalajdzievski2023_profile`](#equimo-finetune-rslora-kalajdzievski2023-profile)
- [`soft_prompt_lester2021_profile`](#equimo-finetune-soft-prompt-lester2021-profile)
- [`ssf_lian2022_vit_profile`](#equimo-finetune-ssf-lian2022-vit-profile)
- [`StagePolicy`](#equimo-finetune-stagepolicy)
- [`supervised_after_ssl`](#equimo-finetune-supervised-after-ssl)
- [`supervised_after_ssl_trainable_spec`](#equimo-finetune-supervised-after-ssl-trainable-spec)
- [`SupervisedAfterSSLConfig`](#equimo-finetune-supervisedaftersslconfig)
- [`task_adapter_bank`](#equimo-finetune-task-adapter-bank)
- [`vera_kopiczko2024_profile`](#equimo-finetune-vera-kopiczko2024-profile)
- [`vpt_deep`](#equimo-finetune-vpt-deep)
- [`vpt_deep_jia2022_profile`](#equimo-finetune-vpt-deep-jia2022-profile)
- [`vpt_shallow_jia2022_profile`](#equimo-finetune-vpt-shallow-jia2022-profile)

<!-- equimo.finetune:adapter_houlsby2019_profile -->
<a id="equimo-finetune-adapter-houlsby2019-profile"></a>
## `adapter_houlsby2019_profile`

```python
equimo.finetune.adapter_houlsby2019_profile(config: 'AdapterConfig | None' = None) -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return the Houlsby bottleneck-adapter profile declaration.

<!-- equimo.finetune:adapter_transformer -->
<a id="equimo-finetune-adapter-transformer"></a>
## `adapter_transformer`

```python
equimo.finetune.adapter_transformer(model: 'PyTree', *, key: 'jax.Array', bottleneck: 'int | None' = None, placement: 'AdapterPlacement' = 'after_mlp') -> 'PyTree'
```

Defined in `equimo.finetune.recipes`.

> Apply a baseline transformer adapter configuration.

<!-- equimo.finetune:adapter_transformer_strong -->
<a id="equimo-finetune-adapter-transformer-strong"></a>
## `adapter_transformer_strong`

```python
equimo.finetune.adapter_transformer_strong(model: 'PyTree', *, key: 'jax.Array', bottleneck: 'int' = 64) -> 'PyTree'
```

Defined in `equimo.finetune.recipes`.

> Apply a stronger two-placement adapter configuration.

<!-- equimo.finetune:adapterfusion_pfeiffer2021_profile -->
<a id="equimo-finetune-adapterfusion-pfeiffer2021-profile"></a>
## `adapterfusion_pfeiffer2021_profile`

```python
equimo.finetune.adapterfusion_pfeiffer2021_profile(config: 'AdapterFusionConfig | None' = None) -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return the AdapterFusion attention-composition profile declaration.

<!-- equimo.finetune:adaptformer_chen2022_profile -->
<a id="equimo-finetune-adaptformer-chen2022-profile"></a>
## `adaptformer_chen2022_profile`

```python
equimo.finetune.adaptformer_chen2022_profile(config: 'AdaptFormerConfig | None' = None) -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return the AdaptFormer paper-profile declaration.

<!-- equimo.finetune:adaptformer_transformer -->
<a id="equimo-finetune-adaptformer-transformer"></a>
## `adaptformer_transformer`

```python
equimo.finetune.adaptformer_transformer(model: 'PyTree', *, key: 'jax.Array', bottleneck: 'int' = 64) -> 'PyTree'
```

Defined in `equimo.finetune.recipes`.

> Apply AdaptFormer-style adapters.

<!-- equimo.finetune:attention_pool_probe -->
<a id="equimo-finetune-attention-pool-probe"></a>
## `attention_pool_probe`

```python
equimo.finetune.attention_pool_probe(backbone: 'PyTree', *, in_features: 'int', out_features: 'int', key: 'jax.Array', n_last_blocks: 'int | None' = None, embed_dim: 'int' = 512, num_heads: 'int' = 8, dropout: 'float' = 0.0, prepend_cls_token: 'bool' = False, l2_normalize_cls: 'bool' = False) -> 'AttentionPoolingProbe'
```

Defined in `equimo.finetune.recipes`.

> Create a FINO-style attention-pooling probe wrapper.

<!-- equimo.finetune:available_profile_ids -->
<a id="equimo-finetune-available-profile-ids"></a>
## `available_profile_ids`

```python
equimo.finetune.available_profile_ids() -> 'tuple[str, ...]'
```

Defined in `equimo.finetune.profiles`.

> Return the built-in profile identifiers exposed by Equimo.

<!-- equimo.finetune:boft_liu2024_profile -->
<a id="equimo-finetune-boft-liu2024-profile"></a>
## `boft_liu2024_profile`

```python
equimo.finetune.boft_liu2024_profile(config: 'OrthogonalAdapterConfig') -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return the BOFT butterfly-Cayley profile declaration.

<!-- equimo.finetune:continued_ssl_adaptation -->
<a id="equimo-finetune-continued-ssl-adaptation"></a>
## `continued_ssl_adaptation`

```python
equimo.finetune.continued_ssl_adaptation(model: 'PyTree', config: 'ContinuedSSLAdaptationConfig | None' = None, *, key: 'jax.Array') -> 'ContinuedSSLPlan'
```

Defined in `equimo.finetune.continued_ssl`.

> Apply PEFT wrappers and prepare a continued-SSL trainability plan.

<!-- equimo.finetune:continued_ssl_trainable_spec -->
<a id="equimo-finetune-continued-ssl-trainable-spec"></a>
## `continued_ssl_trainable_spec`

```python
equimo.finetune.continued_ssl_trainable_spec(model: 'PyTree', config: 'ContinuedSSLAdaptationConfig | None' = None) -> 'TrainableSpec'
```

Defined in `equimo.finetune.continued_ssl`.

> Build the trainability mask for continued SSL adaptation.

<!-- equimo.finetune:ContinuedSSLAdaptationConfig -->
<a id="equimo-finetune-continuedssladaptationconfig"></a>
## `ContinuedSSLAdaptationConfig`

```python
class equimo.finetune.ContinuedSSLAdaptationConfig(peft: 'LoRAConfig | None' = <factory>, unfreeze_blocks: 'int' = 1, unfreeze_policy: 'UnfreezePolicy' = 'last', train_norm: 'bool' = True, train_head: 'bool' = False, save_stage: 'str' = 'continued_ssl_delta') -> None
```

Defined in `equimo.finetune.continued_ssl`.

> Configuration for target-domain continued SSL adaptation.

<!-- equimo.finetune:ContinuedSSLPlan -->
<a id="equimo-finetune-continuedsslplan"></a>
## `ContinuedSSLPlan`

```python
class equimo.finetune.ContinuedSSLPlan(plan: 'FineTunePlan', stage: 'str', metadata: 'Mapping[str, Any]' = <factory>) -> None
```

Defined in `equimo.finetune.continued_ssl`.

> Fine-tuning plan plus continued-SSL serialization metadata.

<!-- equimo.finetune:dora_factored_2026_profile -->
<a id="equimo-finetune-dora-factored-2026-profile"></a>
## `dora_factored_2026_profile`

```python
equimo.finetune.dora_factored_2026_profile(config: 'DoRAConfig | None' = None) -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return the Scaling DoRA factored-norm profile declaration.

<!-- equimo.finetune:dora_liu2024_profile -->
<a id="equimo-finetune-dora-liu2024-profile"></a>
## `dora_liu2024_profile`

```python
equimo.finetune.dora_liu2024_profile(config: 'DoRAConfig | None' = None) -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return the differentiable DoRA paper-equation profile declaration.

<!-- equimo.finetune:dora_nvlabs_reference_profile -->
<a id="equimo-finetune-dora-nvlabs-reference-profile"></a>
## `dora_nvlabs_reference_profile`

```python
equimo.finetune.dora_nvlabs_reference_profile(config: 'DoRAConfig | None' = None) -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return the NVLabs/HF PEFT DoRA stop-gradient reference profile.

<!-- equimo.finetune:eva_initializer_profile -->
<a id="equimo-finetune-eva-initializer-profile"></a>
## `eva_initializer_profile`

```python
equimo.finetune.eva_initializer_profile(config: 'EVAInitializerConfig') -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return the EVA initializer profile declaration.

<!-- equimo.finetune:FineTuneStage -->
<a id="equimo-finetune-finetunestage"></a>
## `FineTuneStage`

```python
class equimo.finetune.FineTuneStage(name: 'str', model: 'PyTree', plan: 'FineTunePlan') -> None
```

Defined in `equimo.finetune.recipes`.

> Prepared model and plan for one external training stage.

<!-- equimo.finetune:fourierft_gao2024_profile -->
<a id="equimo-finetune-fourierft-gao2024-profile"></a>
## `fourierft_gao2024_profile`

```python
equimo.finetune.fourierft_gao2024_profile(config: 'FourierFTConfig') -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return the FourierFT profile declaration.

<!-- equimo.finetune:full_ft_llrd -->
<a id="equimo-finetune-full-ft-llrd"></a>
## `full_ft_llrd`

```python
equimo.finetune.full_ft_llrd(model: 'PyTree', *, decay: 'float' = 0.75, freeze_patch_embed: 'bool' = True, labels: 'LLRDConfig | None' = None, tagger: 'Tagger' = <function canonical_tags_for_path>) -> 'FineTunePlan'
```

Defined in `equimo.finetune.recipes`.

> Prepare a full fine-tuning plan with LLRD labels.

<!-- equimo.finetune:head_plus_norm -->
<a id="equimo-finetune-head-plus-norm"></a>
## `head_plus_norm`

```python
equimo.finetune.head_plus_norm(model: 'PyTree', config: 'HeadPlusNormConfig | None' = None, *, labels: 'LLRDConfig | None' = None, tagger: 'Tagger' = <function canonical_tags_for_path>) -> 'FineTunePlan'
```

Defined in `equimo.finetune.recipes`.

> Prepare a head-plus-norm fine-tuning plan.

<!-- equimo.finetune:HeadPlusNormConfig -->
<a id="equimo-finetune-headplusnormconfig"></a>
## `HeadPlusNormConfig`

```python
class equimo.finetune.HeadPlusNormConfig(train_head: 'bool' = True, train_norm: 'bool' = True, norm_types: 'tuple[str, ...]' = ('LayerNorm', 'RMSNorm', 'BatchNorm', 'GroupNorm'), train_norm_scale: 'bool' = True, train_norm_bias: 'bool' = True, train_bias: 'bool' = False, bn_stats_policy: 'str' = 'frozen', include_embeddings: 'bool' = False, include_positional_parameters: 'bool' = False) -> None
```

Defined in `equimo.finetune.recipes`.

> Configuration metadata for head-plus-norm tuning.

<!-- equimo.finetune:ia3_liu2022_profile -->
<a id="equimo-finetune-ia3-liu2022-profile"></a>
## `ia3_liu2022_profile`

```python
equimo.finetune.ia3_liu2022_profile(config: 'IA3Config | None' = None) -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return the Liu et al. IA3 activation-scaling profile declaration.

<!-- equimo.finetune:linear_probe -->
<a id="equimo-finetune-linear-probe"></a>
## `linear_probe`

```python
equimo.finetune.linear_probe(backbone: 'PyTree', *, in_features: 'int', out_features: 'int', key: 'jax.Array', pool: 'PoolName' = 'auto') -> 'LinearProbe'
```

Defined in `equimo.finetune.recipes`.

> Create a linear-probe model wrapper.

<!-- equimo.finetune:LinearProbeConfig -->
<a id="equimo-finetune-linearprobeconfig"></a>
## `LinearProbeConfig`

```python
class equimo.finetune.LinearProbeConfig(pool: 'str' = 'auto', feature_norm: 'str | None' = None, head: 'str' = 'linear', head_bias: 'bool' = True, head_init: 'str' = 'trunc_normal_0.02', train_backbone: 'bool' = False, train_head: 'bool' = True, cache_features: 'bool' = False) -> None
```

Defined in `equimo.finetune.recipes`.

> Configuration metadata for linear probing.

<!-- equimo.finetune:LinearProbeRecipe -->
<a id="equimo-finetune-linearproberecipe"></a>
## `LinearProbeRecipe`

```python
class equimo.finetune.LinearProbeRecipe(pool: 'str' = 'auto', feature_norm: 'str' = 'l2_or_standardize', head: 'str' = 'linear') -> None
```

Defined in `equimo.finetune.recipes`.

> Recipe metadata for linear probing.

<!-- equimo.finetune:lora_equimo_default_profile -->
<a id="equimo-finetune-lora-equimo-default-profile"></a>
## `lora_equimo_default_profile`

```python
equimo.finetune.lora_equimo_default_profile(config: 'LoRAConfig | None' = None) -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return Equimo's safe-default LoRA profile declaration.

<!-- equimo.finetune:lora_fa_zhang2026_profile -->
<a id="equimo-finetune-lora-fa-zhang2026-profile"></a>
## `lora_fa_zhang2026_profile`

```python
equimo.finetune.lora_fa_zhang2026_profile(config: 'LoRAFAConfig | None' = None) -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return the corrected LoRA-FA v3 profile declaration.

<!-- equimo.finetune:lora_hu2021_qv_profile -->
<a id="equimo-finetune-lora-hu2021-qv-profile"></a>
## `lora_hu2021_qv_profile`

```python
equimo.finetune.lora_hu2021_qv_profile(config: 'LoRAConfig | None' = None) -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return the Hu et al. Q/V projection-segment LoRA profile declaration.

<!-- equimo.finetune:lora_transformer -->
<a id="equimo-finetune-lora-transformer"></a>
## `lora_transformer`

```python
equimo.finetune.lora_transformer(model: 'PyTree', *, key: 'jax.Array', rank: 'int' = 8, alpha: 'float' = 16.0, target: 'tuple[str, ...]' = ('attention.qkv', 'attention.proj')) -> 'PyTree'
```

Defined in `equimo.finetune.recipes`.

> Apply the default transformer LoRA recipe.

<!-- equimo.finetune:lora_transformer_all_linear -->
<a id="equimo-finetune-lora-transformer-all-linear"></a>
## `lora_transformer_all_linear`

```python
equimo.finetune.lora_transformer_all_linear(model: 'PyTree', *, key: 'jax.Array', rank: 'int' = 8, alpha: 'float' = 16.0) -> 'PyTree'
```

Defined in `equimo.finetune.recipes`.

> Apply LoRA to every linear module selected by the generic predicate.

<!-- equimo.finetune:lpft -->
<a id="equimo-finetune-lpft"></a>
## `lpft`

```python
equimo.finetune.lpft(*, stage1: 'TrainableSpec | None' = None, stage2: 'TrainableSpec | None' = None, stage2_labels: 'LLRDConfig | None' = None, preserve_trained_head: 'bool' = True, stage1_policy: 'StagePolicy | None' = None, head_selector: 'str | TargetSpec' = 'head') -> 'LPFTRecipe'
```

Defined in `equimo.finetune.recipes`.

> Return LP-FT stage metadata.

<!-- equimo.finetune:LPFTRecipe -->
<a id="equimo-finetune-lpftrecipe"></a>
## `LPFTRecipe`

```python
class equimo.finetune.LPFTRecipe(stage1: 'TrainableSpec' = <factory>, stage2: 'TrainableSpec' = <factory>, preserve_trained_head: 'bool' = True, stage2_labels: 'LLRDConfig' = <factory>, stage1_policy: 'StagePolicy' = <factory>, head_selector: 'str | TargetSpec' = 'head', external_ft_lr_scale_hint: 'tuple[float, float]' = (0.2, 0.5)) -> None
```

Defined in `equimo.finetune.recipes`.

> Linear-probe then fine-tune recipe metadata.

<!-- equimo.finetune:oft_qiu2023_profile -->
<a id="equimo-finetune-oft-qiu2023-profile"></a>
## `oft_qiu2023_profile`

```python
equimo.finetune.oft_qiu2023_profile(config: 'OrthogonalAdapterConfig | None' = None) -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return the OFT Cayley-transform profile declaration.

<!-- equimo.finetune:partial_ft_last_k_blocks -->
<a id="equimo-finetune-partial-ft-last-k-blocks"></a>
## `partial_ft_last_k_blocks`

```python
equimo.finetune.partial_ft_last_k_blocks(model: 'PyTree', *, k: 'int | str' = 'one_third', decay: 'float' = 0.75, train_head: 'bool' = True, train_norm: 'bool' = True, freeze_patch_embed: 'bool' = True, labels: 'LLRDConfig | None' = None, tagger: 'Tagger' = <function canonical_tags_for_path>) -> 'FineTunePlan'
```

Defined in `equimo.finetune.recipes`.

> Prepare a partial fine-tuning plan over the last ``k`` blocks.

<!-- equimo.finetune:partial_unfreeze -->
<a id="equimo-finetune-partial-unfreeze"></a>
## `partial_unfreeze`

```python
equimo.finetune.partial_unfreeze(model: 'PyTree', config: 'PartialUnfreezeConfig | None' = None, *, labels: 'LLRDConfig | None' = None, tagger: 'Tagger' = <function canonical_tags_for_path>) -> 'FineTunePlan'
```

Defined in `equimo.finetune.recipes`.

> Prepare a partial-unfreeze plan from ``PartialUnfreezeConfig``.

<!-- equimo.finetune:PartialUnfreezeConfig -->
<a id="equimo-finetune-partialunfreezeconfig"></a>
## `PartialUnfreezeConfig`

```python
class equimo.finetune.PartialUnfreezeConfig(span: "Literal['last']" = 'last', fraction: 'float' = 0.3333333333333333, min_blocks: 'int' = 1, train_head: 'bool' = True, train_norm: 'bool' = True, train_embeddings: 'bool' = False, train_positional_parameters: 'bool' = False) -> None
```

Defined in `equimo.finetune.recipes`.

> Configuration metadata for partial unfreezing.

<!-- equimo.finetune:pissa_meng2024_profile -->
<a id="equimo-finetune-pissa-meng2024-profile"></a>
## `pissa_meng2024_profile`

```python
equimo.finetune.pissa_meng2024_profile(config: 'PiSSAConfig | None' = None) -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return the PiSSA exact-SVD initialization profile declaration.

<!-- equimo.finetune:prefix_tuning_li2021_profile -->
<a id="equimo-finetune-prefix-tuning-li2021-profile"></a>
## `prefix_tuning_li2021_profile`

```python
equimo.finetune.prefix_tuning_li2021_profile(config: 'PrefixConfig | None' = None) -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return the Li and Liang attention-prefix profile declaration.

<!-- equimo.finetune:prepare_lpft_stage1_model -->
<a id="equimo-finetune-prepare-lpft-stage1-model"></a>
## `prepare_lpft_stage1_model`

```python
equimo.finetune.prepare_lpft_stage1_model(model: 'PyTree', *, policy: 'StagePolicy | None' = None, head_selector: 'str | TargetSpec' = 'head', tagger: 'Tagger' = <function canonical_tags_for_path>) -> 'PyTree'
```

Defined in `equimo.finetune.recipes`.

> Return the model used for LP-FT stage 1.

<!-- equimo.finetune:prepare_lpft_stage2_model -->
<a id="equimo-finetune-prepare-lpft-stage2-model"></a>
## `prepare_lpft_stage2_model`

```python
equimo.finetune.prepare_lpft_stage2_model(base_model: 'PyTree', trained_stage1_model: 'PyTree', *, head_selector: 'str | TargetSpec' = 'head', tagger: 'Tagger' = <function canonical_tags_for_path>) -> 'PyTree'
```

Defined in `equimo.finetune.recipes`.

> Return the LP-FT stage-2 model with the trained stage-1 head transferred.

<!-- equimo.finetune:ptuning_v2_liu2022_profile -->
<a id="equimo-finetune-ptuning-v2-liu2022-profile"></a>
## `ptuning_v2_liu2022_profile`

```python
equimo.finetune.ptuning_v2_liu2022_profile(config: 'PTuningV2Config | None' = None) -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return the P-Tuning v2 deep-prompt profile declaration.

<!-- equimo.finetune:randlora_profile -->
<a id="equimo-finetune-randlora-profile"></a>
## `randlora_profile`

```python
equimo.finetune.randlora_profile(config: 'RandLoRAConfig | None' = None) -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return the RandLoRA frozen-random-basis profile.

<!-- equimo.finetune:recipes -->
<a id="equimo-finetune-recipes"></a>
## `recipes`

```python
module equimo.finetune.recipes
```

Defined in `equimo.finetune.recipes`.

> Baseline fine-tuning recipes.

<!-- equimo.finetune:rslora_kalajdzievski2023_profile -->
<a id="equimo-finetune-rslora-kalajdzievski2023-profile"></a>
## `rslora_kalajdzievski2023_profile`

```python
equimo.finetune.rslora_kalajdzievski2023_profile(config: 'RsLoRAConfig | None' = None) -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return the rank-stabilized LoRA profile declaration.

<!-- equimo.finetune:soft_prompt_lester2021_profile -->
<a id="equimo-finetune-soft-prompt-lester2021-profile"></a>
## `soft_prompt_lester2021_profile`

```python
equimo.finetune.soft_prompt_lester2021_profile(config: 'SoftPromptConfig | None' = None) -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return the Lester et al. input soft-prompt profile declaration.

<!-- equimo.finetune:ssf_lian2022_vit_profile -->
<a id="equimo-finetune-ssf-lian2022-vit-profile"></a>
## `ssf_lian2022_vit_profile`

```python
equimo.finetune.ssf_lian2022_vit_profile(config: 'ScaleShiftConfig | None' = None) -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return the Lian et al. SSF ViT profile declaration.

<!-- equimo.finetune:StagePolicy -->
<a id="equimo-finetune-stagepolicy"></a>
## `StagePolicy`

```python
class equimo.finetune.StagePolicy(deterministic_frozen_backbone: 'bool' = True, disable_stochastic_depth: 'bool' = True, disable_dropout: 'bool' = True) -> None
```

Defined in `equimo.finetune.recipes`.

> Model policy applied before building a fine-tuning stage plan.

<!-- equimo.finetune:supervised_after_ssl -->
<a id="equimo-finetune-supervised-after-ssl"></a>
## `supervised_after_ssl`

```python
equimo.finetune.supervised_after_ssl(model: 'PyTree', config: 'SupervisedAfterSSLConfig | None' = None, *, key: 'jax.Array | None' = None) -> 'ContinuedSSLPlan'
```

Defined in `equimo.finetune.continued_ssl`.

> Prepare the supervised stage after a continued-SSL delta.

<!-- equimo.finetune:supervised_after_ssl_trainable_spec -->
<a id="equimo-finetune-supervised-after-ssl-trainable-spec"></a>
## `supervised_after_ssl_trainable_spec`

```python
equimo.finetune.supervised_after_ssl_trainable_spec(config: 'SupervisedAfterSSLConfig | None' = None) -> 'TrainableSpec'
```

Defined in `equimo.finetune.continued_ssl`.

> Build the supervised follow-up trainability mask.

<!-- equimo.finetune:SupervisedAfterSSLConfig -->
<a id="equimo-finetune-supervisedaftersslconfig"></a>
## `SupervisedAfterSSLConfig`

```python
class equimo.finetune.SupervisedAfterSSLConfig(peft: 'LoRAConfig | None' = <factory>, train_head: 'bool' = True, reuse_ssl_delta: 'bool' = True) -> None
```

Defined in `equimo.finetune.continued_ssl`.

> Configuration for supervised fine-tuning after continued SSL.

<!-- equimo.finetune:task_adapter_bank -->
<a id="equimo-finetune-task-adapter-bank"></a>
## `task_adapter_bank`

```python
equimo.finetune.task_adapter_bank(model: 'PyTree', *, key: 'jax.Array', names: 'tuple[str, ...]' = ('default',), bottleneck: 'int' = 64, placement: 'AdapterPlacement' = 'after_mlp') -> 'PyTree'
```

Defined in `equimo.finetune.recipes`.

> Add a named adapter bank for one or more tasks.

<!-- equimo.finetune:vera_kopiczko2024_profile -->
<a id="equimo-finetune-vera-kopiczko2024-profile"></a>
## `vera_kopiczko2024_profile`

```python
equimo.finetune.vera_kopiczko2024_profile(config: 'VeRAConfig | None' = None) -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return Equimo's shape-compatible VeRA profile declaration.

<!-- equimo.finetune:vpt_deep -->
<a id="equimo-finetune-vpt-deep"></a>
## `vpt_deep`

```python
equimo.finetune.vpt_deep(model: 'PyTree', *, key: 'jax.Array', num_tokens: 'int' = 10) -> 'PromptedModel'
```

Defined in `equimo.finetune.recipes`.

> Apply the default deep visual prompt-tuning recipe.

<!-- equimo.finetune:vpt_deep_jia2022_profile -->
<a id="equimo-finetune-vpt-deep-jia2022-profile"></a>
## `vpt_deep_jia2022_profile`

```python
equimo.finetune.vpt_deep_jia2022_profile(config: 'VPTDeepConfig | None' = None) -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return the VPT-Deep profile declaration.

<!-- equimo.finetune:vpt_shallow_jia2022_profile -->
<a id="equimo-finetune-vpt-shallow-jia2022-profile"></a>
## `vpt_shallow_jia2022_profile`

```python
equimo.finetune.vpt_shallow_jia2022_profile(config: 'VPTShallowConfig | None' = None) -> 'MethodProfile'
```

Defined in `equimo.finetune.profiles`.

> Return the VPT-Shallow profile declaration.
