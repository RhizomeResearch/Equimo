<!-- Generated by docs/finetuning/generate_api_reference.py. Do not edit. -->

# Regularization

Regularization configurations, losses, and feature-tap helpers.

[Fine-tuning overview](../index.md) | [API reference index](index.md)

## Symbols

- [`adalora_orthogonality_aux_loss_spec`](#equimo-finetune-adalora-orthogonality-aux-loss-spec)
- [`adalora_orthogonality_loss`](#equimo-finetune-adalora-orthogonality-loss)
- [`adapter_norm_loss`](#equimo-finetune-adapter-norm-loss)
- [`delta_attention_aux_loss_spec`](#equimo-finetune-delta-attention-aux-loss-spec)
- [`delta_attention_loss`](#equimo-finetune-delta-attention-loss)
- [`DELTAAttentionConfig`](#equimo-finetune-deltaattentionconfig)
- [`ewc_loss`](#equimo-finetune-ewc-loss)
- [`EWCConfig`](#equimo-finetune-ewcconfig)
- [`feature_distillation_loss`](#equimo-finetune-feature-distillation-loss)
- [`feature_distillation_loss_from_taps`](#equimo-finetune-feature-distillation-loss-from-taps)
- [`FeatureDistillationConfig`](#equimo-finetune-featuredistillationconfig)
- [`l2_sp_loss`](#equimo-finetune-l2-sp-loss)
- [`L2SPConfig`](#equimo-finetune-l2spconfig)
- [`mixout_leaf`](#equimo-finetune-mixout-leaf)
- [`mixout_tree`](#equimo-finetune-mixout-tree)
- [`MixoutConfig`](#equimo-finetune-mixoutconfig)
- [`select_feature_taps`](#equimo-finetune-select-feature-taps)
- [`task_vector_norm_loss`](#equimo-finetune-task-vector-norm-loss)

<!-- equimo.finetune:adalora_orthogonality_aux_loss_spec -->
<a id="equimo-finetune-adalora-orthogonality-aux-loss-spec"></a>
## `adalora_orthogonality_aux_loss_spec`

```python
equimo.finetune.adalora_orthogonality_aux_loss_spec(*, coefficient_hint: 'float' = 0.5) -> 'AuxLossSpec'
```

Defined in `equimo.finetune.regularization`.

> Return the explicit AuxLossSpec for AdaLoRA orthogonality regularization.

<!-- equimo.finetune:adalora_orthogonality_loss -->
<a id="equimo-finetune-adalora-orthogonality-loss"></a>
## `adalora_orthogonality_loss`

```python
equimo.finetune.adalora_orthogonality_loss(model: 'PyTree', *, coefficient: 'float | None' = None) -> 'jax.Array'
```

Defined in `equimo.finetune.regularization`.

> Return AdaLoRA triplet orthogonality loss over all adapters.

<!-- equimo.finetune:adapter_norm_loss -->
<a id="equimo-finetune-adapter-norm-loss"></a>
## `adapter_norm_loss`

```python
equimo.finetune.adapter_norm_loss(model: 'PyTree', *, tags: 'tuple[str, ...]' = ('adapter', 'lora', 'dora', 'prompt', 'prefix', 'ia3', 'scale_shift', 'vera'), reduction: "Literal['mean', 'sum']" = 'mean', coefficient: 'float | None' = None) -> 'jax.Array'
```

Defined in `equimo.finetune.regularization`.

> Return an optionally scaled norm penalty over adapter/PEFT leaves.

<!-- equimo.finetune:delta_attention_aux_loss_spec -->
<a id="equimo-finetune-delta-attention-aux-loss-spec"></a>
## `delta_attention_aux_loss_spec`

```python
equimo.finetune.delta_attention_aux_loss_spec(*, coefficient_hint: 'float' = 1.0) -> 'AuxLossSpec'
```

Defined in `equimo.finetune.regularization`.

> Return the explicit AuxLossSpec for DELTA attention regularization.

<!-- equimo.finetune:delta_attention_loss -->
<a id="equimo-finetune-delta-attention-loss"></a>
## `delta_attention_loss`

```python
equimo.finetune.delta_attention_loss(student_features: 'Mapping | tuple | list | jax.Array', teacher_features: 'Mapping | tuple | list | jax.Array', attention_weights: 'Mapping | tuple | list | jax.Array', *, config: 'DELTAAttentionConfig | None' = None, coefficient: 'float | None' = None, reduction: "Literal['sum', 'mean'] | None" = None) -> 'jax.Array'
```

Defined in `equimo.finetune.regularization`.

> Return DELTA attention-weighted feature-map transfer loss.
>
> The default layout follows the DELTA reference implementation: feature maps
> are shaped ``(batch, channels, *spatial)`` and attention weights are
> per-channel vectors.

<!-- equimo.finetune:DELTAAttentionConfig -->
<a id="equimo-finetune-deltaattentionconfig"></a>
## `DELTAAttentionConfig`

```python
class equimo.finetune.DELTAAttentionConfig(layers: 'tuple[str, ...]' = ('100%',), attention: 'str' = 'learned_feature_map_attention', feature_alignment: 'Mapping[str, Any]' = <factory>, teacher: 'str' = 'pretrained_anchor', stop_gradient_teacher: 'bool' = True, reduction: "Literal['sum', 'mean']" = 'sum') -> None
```

Defined in `equimo.finetune.regularization`.

> DELTA-style attention-weighted feature-map transfer metadata.

<!-- equimo.finetune:ewc_loss -->
<a id="equimo-finetune-ewc-loss"></a>
## `ewc_loss`

```python
equimo.finetune.ewc_loss(model: 'PyTree', anchor: 'PyTree', fisher: 'PyTree', *, config: 'EWCConfig | None' = None, coefficient: 'float | None' = None, reduction: "Literal['sum', 'mean'] | None" = None) -> 'jax.Array'
```

Defined in `equimo.finetune.regularization`.

> Return an optionally scaled EWC penalty using supplied Fisher diagonals.

<!-- equimo.finetune:EWCConfig -->
<a id="equimo-finetune-ewcconfig"></a>
## `EWCConfig`

```python
class equimo.finetune.EWCConfig(fisher: "Literal['diagonal']" = 'diagonal', reduction: "Literal['sum', 'mean']" = 'sum', coefficient_hint: 'float | None' = None, anchor: 'str' = 'previous_task') -> None
```

Defined in `equimo.finetune.regularization`.

> Elastic Weight Consolidation penalty metadata.

<!-- equimo.finetune:feature_distillation_loss -->
<a id="equimo-finetune-feature-distillation-loss"></a>
## `feature_distillation_loss`

```python
equimo.finetune.feature_distillation_loss(student_features: 'jax.Array', teacher_features: 'jax.Array', *, config: 'FeatureDistillationConfig | None' = None, metric: 'str | None' = None, normalize_features: 'bool | None' = None, stop_gradient_teacher: 'bool | None' = None, reduction: "Literal['mean', 'sum'] | None" = None, coefficient: 'float | None' = None) -> 'jax.Array'
```

Defined in `equimo.finetune.regularization`.

> Return an optionally scaled feature distillation penalty.

<!-- equimo.finetune:feature_distillation_loss_from_taps -->
<a id="equimo-finetune-feature-distillation-loss-from-taps"></a>
## `feature_distillation_loss_from_taps`

```python
equimo.finetune.feature_distillation_loss_from_taps(student_taps: 'Mapping | tuple | list | jax.Array', teacher_taps: 'Mapping | tuple | list | jax.Array', *, config: 'FeatureDistillationConfig | None' = None, metric: 'str | None' = None, normalize_features: 'bool | None' = None, stop_gradient_teacher: 'bool | None' = None, reduction: "Literal['mean', 'sum'] | None" = None, coefficient: 'float | None' = None) -> 'jax.Array'
```

Defined in `equimo.finetune.regularization`.

> Return feature distillation loss after selecting configured taps.

<!-- equimo.finetune:FeatureDistillationConfig -->
<a id="equimo-finetune-featuredistillationconfig"></a>
## `FeatureDistillationConfig`

```python
class equimo.finetune.FeatureDistillationConfig(layers: 'tuple[str, ...]' = ('50%', '100%'), metric: 'str' = 'mse', normalize_features: 'bool' = True, stop_gradient_teacher: 'bool' = True, reduction: "Literal['mean', 'sum']" = 'mean', coefficient_hint: 'float' = 1.0) -> None
```

Defined in `equimo.finetune.regularization`.

> Generic teacher-feature distillation metadata.
>
> This is not a DELTA attention-transfer configuration.

<!-- equimo.finetune:l2_sp_loss -->
<a id="equimo-finetune-l2-sp-loss"></a>
## `l2_sp_loss`

```python
equimo.finetune.l2_sp_loss(model: 'PyTree', reference: 'PyTree', *, config: 'L2SPConfig | None' = None, coefficient: 'float | None' = None, reduction: "Literal['sum', 'mean_per_parameter'] | None" = None) -> 'jax.Array'
```

Defined in `equimo.finetune.regularization`.

> Return an optionally scaled L2-SP penalty between compatible leaves.

<!-- equimo.finetune:L2SPConfig -->
<a id="equimo-finetune-l2spconfig"></a>
## `L2SPConfig`

```python
class equimo.finetune.L2SPConfig(shared_mask: 'str' = 'transferred_trainable', new_mask: 'str' = 'newly_initialized', alpha: 'float' = 0.001, beta: 'float' = 0.0, reduction: "Literal['sum', 'mean_per_parameter']" = 'sum', anchor: 'str' = 'pretrained', library_variant: 'str' = 'paper_objective', coefficient_hint: 'float | None' = None, sweep_hint: 'tuple[float, float]' = (1e-05, 0.1)) -> None
```

Defined in `equimo.finetune.regularization`.

> L2-SP penalty metadata.

<!-- equimo.finetune:mixout_leaf -->
<a id="equimo-finetune-mixout-leaf"></a>
## `mixout_leaf`

```python
equimo.finetune.mixout_leaf(leaf: 'jax.Array', anchor: 'jax.Array', *, key: 'jax.Array', config: 'MixoutConfig | None' = None, p: 'float | None' = None, inference: 'bool' = False) -> 'jax.Array'
```

Defined in `equimo.finetune.regularization`.

> Return a Mixout-sampled leaf anchored to ``anchor``.
>
> The sampled value has expectation equal to ``leaf`` during training and
> returns ``leaf`` unchanged during inference.

<!-- equimo.finetune:mixout_tree -->
<a id="equimo-finetune-mixout-tree"></a>
## `mixout_tree`

```python
equimo.finetune.mixout_tree(tree: 'PyTree', anchor: 'PyTree', *, key: 'jax.Array', config: 'MixoutConfig | None' = None, p: 'float | None' = None, inference: 'bool' = False) -> 'PyTree'
```

Defined in `equimo.finetune.regularization`.

> Apply Mixout to compatible inexact array leaves in ``tree``.

<!-- equimo.finetune:MixoutConfig -->
<a id="equimo-finetune-mixoutconfig"></a>
## `MixoutConfig`

```python
class equimo.finetune.MixoutConfig(p: 'float' = 0.1, anchor: 'str' = 'pretrained', target: 'str' = 'full_or_partial_trainable_weights') -> None
```

Defined in `equimo.finetune.regularization`.

> Mixout metadata for anchored stochastic parameter substitution.

<!-- equimo.finetune:select_feature_taps -->
<a id="equimo-finetune-select-feature-taps"></a>
## `select_feature_taps`

```python
equimo.finetune.select_feature_taps(taps: 'Mapping | tuple | list | jax.Array', layers: 'tuple[str, ...]') -> 'tuple[jax.Array, ...]'
```

Defined in `equimo.finetune.regularization`.

> Select feature taps by name, index string, or percentage selector.

<!-- equimo.finetune:task_vector_norm_loss -->
<a id="equimo-finetune-task-vector-norm-loss"></a>
## `task_vector_norm_loss`

```python
equimo.finetune.task_vector_norm_loss(task_vector: 'PyTree', *, reduction: "Literal['mean', 'sum']" = 'mean', coefficient: 'float | None' = None) -> 'jax.Array'
```

Defined in `equimo.finetune.regularization`.

> Return an optionally scaled norm penalty for a task-vector delta tree.
